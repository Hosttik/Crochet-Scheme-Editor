# Pages smoke verification

This temporary verification document exists to trigger PR CI and confirm that the published GitHub Pages endpoint is reachable after Pages was enabled with GitHub Actions as the source.
